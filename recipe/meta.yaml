{% set name = "pyxel-sim" %}
{% set version = "2.15" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/pyxel_sim-{{ version }}.tar.gz
  sha256: e573d3f4048b13f2d4b7cc431272b282646972e77c8c6430ec04fd0b206b883c

build:
  entry_points:
    - pyxel = pyxel.run:main
    - pyxel-sim = pyxel.run:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.10
    - setuptools >=77
    - versioneer ==0.29
    - wheel
    - pip
  run:
    - python >=3.10
    - numpy >=2
    - scipy >=1.12
    - pandas >=2.1,<3
    - xarray >=2024.10.0
    - astropy >=5.2
    - astroquery <0.4.11
    - specutils >=1.2
    - synphot
#    - pyckles
    - scikit-image >=0.23.0
    - numba >=0.56.4
    - hvplot >=0.9
    - fsspec >=2021
    - pympler
    - pillow
    - diskcache
    - rich
    - annotated-types
    - asdf # Added manually
    - pygmo  >=2.16.1 # Added manually
    - poppy # Added manually
    - panel # Added manually

test:
  imports:
    - pyxel
    - pyxel.calibration
  commands:
#    - pip check
    - pyxel --help
    - pyxel-sim --help
  requires:
    - pip

about:
  home: https://esa.gitlab.io/pyxel/
  summary: Pyxel detector simulation framework.
  dev_url: https://gitlab.com/esa/pyxel
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - flemmel
